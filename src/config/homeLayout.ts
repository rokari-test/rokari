/**
 * Popular section layout on the homepage.
 * - `featured` — Epic-style hero + side list (current)
 * - `carousel` — original horizontal scroll cards
 */
export type PopularLayoutMode = 'featured' | 'carousel'

export const POPULAR_LAYOUT: PopularLayoutMode = 'featured'
